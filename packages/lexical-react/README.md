This is a fork of `@lexical/react`.

The fork only updates the dependency on `@lexical/markdown` to point to
the forked `lexical-markdown` package.

See more details in the [lexical-markdown](../lexical-markdown) readme.
